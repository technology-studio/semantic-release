name: Release
on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write

jobs:
  release:
    name: '/'
    uses: technology-studio/github-workflows/.github/workflows/_release.yml@rsi/npm-oidc
    secrets: inherit
